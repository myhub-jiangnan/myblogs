

```
export default {
  // 姓名
    nameRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('姓名不能为空'));
        } else {
            let nameReg = /(^$)|^[a-zA-Z\u4e00-\u9fa5\·]+$/
            if (!nameReg.test(value)) {
                callback(new Error('不支持数字、特殊符号表情!'))
            } else {
                callback()
            }
        }
    },
    // 账户
    accountName(rule, value, callback) {
        if (!value) {
            return callback(new Error('账户名不能为空'));
        } else {
            let nameReg = /(^$)|^[a-zA-Z\u4e00-\u9fa5\·]+$/
            if (!nameReg.test(value)) {
                callback(new Error('不支持数字、特殊符号表情!'))
            } else {
                callback()
            }
        }
    },
    // 工种
    workerType(rule, value, callback) {
        if (!value) {
            return callback(new Error('工种不能为空'));
        } else {
            let nameReg = /(^$)|^[a-zA-Z\u4e00-\u9fa5\·]+$/
            if (!nameReg.test(value)) {
                callback(new Error('不支持数字、特殊符号表情!'))
            } else {
                callback()
            }
        }
    },
    // 身份证
    idCardRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('身份证号未录入!'));
        } else {
            let idCardReg = /(^$)|(^\d{15}$)|(^\d{18}$)|(^\d{17}(\d|X|x)$)/
            if (!idCardReg.test(value)) {
                callback(new Error('您输入的身份证号码不正确，请重新输入!'))
            } else {
                callback()
            }
        }
    },
    // 手机号验证
    mobileRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('手机号不能为空'));
        } else {
            let mobileReg = /^[1][3,4,5,7,8][0-9]{9}$/;
            if (!mobileReg.test(value)) {
                callback(new Error('请输入正确格式的手机号'));
            } else {
                callback();
            }
        }
    },
    // 密码验证
    psRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('请设置密码'));
        } else {
            let psReg = /(?=.*[a-z])|(?=.*[A-Z])|(?=.*[\W]).{7,}/
            if (!psReg.test(value)) {
                callback(new Error('8位以上字符，必须包含大、小写字母、数字和特殊符号4项中至少3项'));
            } else {
                callback();
            }
        }
    },
    // 验证码验证
    verifyCodeRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('请输入验证码!'));
        } else {
            let verifyCodeReg = /^\d{4}$/
            if (!verifyCodeReg.test(value)) {
                callback(new Error('您输入的验证码有误'));
            } else {
                callback()
            }
        }
    },
    // 应发工资
    salaryRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('应发工资未录入!'));
        } else {
            let salaryReg = /^[0-9]*$/
            if (!salaryReg.test(value)) {
                callback(new Error('仅支持输入数字!'))
            }
        }
    },
    // 银行卡号
    bankCardNumRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('银行卡号未录入!'));
        } else {
            let salaryReg = /^[0-9]*$/
            if (!salaryReg.test(value)) {
                callback(new Error('仅支持输入数字!'))
            }
        }
    },
    // 发卡行
    bankNameRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('发卡行未录入!'));
        }
    },
    // 开户支行
    openingBankRules(rule, value, callback) {
        if (!value) {
            return callback(new Error('开户支行未录入!'));
        }
    },






}

```